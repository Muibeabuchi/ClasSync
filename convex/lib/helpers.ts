// import { payment_plans } from '../constants/payment';
import { lecturerCurrentPlan } from '../schema';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const _plans = Object.values(lecturerCurrentPlan);

// export function planMapper(planCode: string): (typeof _plans)[number] {
//   if (planCode === payment_plans.LECTURER_BASIC_PLAN) {
//     return 'BASIC';
//   }
//   return 'PRO';
// }
